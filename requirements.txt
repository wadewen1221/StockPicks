# 智能A股投助手 V2 依赖
#
# ⚠️ 此文件原包含前端 Vue 依赖(vue/element-plus/axios),前端包不在 PyPI,
#    会导致 'pip install -r requirements.txt' 在 CI 上失败。
#    已拆分为:
#      - requirements-backend.txt  → 后端 Python 依赖 (Tornado/akshare/...)
#      - frontend/package.json     → 前端依赖 (vue/element-plus/axios)
#
# 一键安装(开发/CI 用):  pip install -r requirements-backend.txt
# 仅后端部署:            pip install -r requirements-backend.txt
